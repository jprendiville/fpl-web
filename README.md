# React + TypeScript + Vite

# Go into the repo:
cd fpl-web
# Run the Vite React+TS setup:
npm create vite@latest . -- --template react-ts
# Install dependencies:
npm install
# Test run:
npm run dev

